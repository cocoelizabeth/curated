import merge from 'lodash/merge';

const topicsReducer = (state={}, action) => {
    debugger
    Object.freeze(state);
    switch(action.type) {
        default:
            return state;
    }
};

export default topicsReducer;